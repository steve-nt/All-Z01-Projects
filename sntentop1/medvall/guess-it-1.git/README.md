# Number guesser

Program that given a number as standard input, prints out a range in which the next number provided should be. Abuses rounding in tester's score calculation for approximately 5% extra points.

## Build

```bash
./build.sh
```

## Usage

After building the program, copy the student directory into the tester directory and follow README in tester directory for further instructions.